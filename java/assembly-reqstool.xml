<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
              https://maven.apache.org/xsd/assembly-2.2.0.xsd">
  <id>reqstool</id>
  <formats>
    <format>zip</format>
  </formats>
  <includeBaseDirectory>true</includeBaseDirectory>
  <!-- Must match finalName pattern expected by reqstool MavenLocation/extract_zip:
       exactly one top-level dir containing the dataset files. -->
  <baseDirectory>${project.build.finalName}-reqstool</baseDirectory>

  <fileSets>
    <!-- reqstool dataset files at zip root -->
    <fileSet>
      <directory>${project.basedir}</directory>
      <includes>
        <include>requirements.yml</include>
        <include>software_verification_cases.yml</include>
        <include>manual_verification_results.yml</include>
        <include>annotations.yml</include>
        <include>reqstool_config.yml</include>
      </includes>
      <outputDirectory>.</outputDirectory>
    </fileSet>
    <!-- JUnit XML test results -->
    <fileSet>
      <directory>${project.basedir}/test_results</directory>
      <includes>
        <include>**/*.xml</include>
      </includes>
      <outputDirectory>test_results</outputDirectory>
    </fileSet>
  </fileSets>
</assembly>
