on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    name: Validate (${{ matrix.target }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - fixtures/parent
          - fixtures/ecosystems/java
          - fixtures/ecosystems/python
          - fixtures/ecosystems/typescript
    steps:
      - uses: actions/checkout@v7

      - name: Check target exists
        id: check
        run: |
          if [ -d "${{ matrix.target }}" ]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/setup-python@v6
        if: steps.check.outputs.exists == 'true'
        with:
          python-version: "3.14"

      - name: Install reqstool
        if: steps.check.outputs.exists == 'true'
        run: pip install reqstool

      - name: Validate ${{ matrix.target }}
        if: steps.check.outputs.exists == 'true'
        run: reqstool status local -p ${{ matrix.target }}
